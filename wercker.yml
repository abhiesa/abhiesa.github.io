box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: abhiesa/abhiesa.github.io
        path: public
