box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        flags: --disableSitemap=true
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: abhiesa/abhiesa.github.io
        path: public
        domain: github.com
